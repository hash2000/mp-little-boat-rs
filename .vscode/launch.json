{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug client",
            "program": "${workspaceFolder}/target/debug/little-boat-client",
            "args": ["--your-flag", "value"],  // Параметры командной строки
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "rust: cargo build"
        }
    ]
}