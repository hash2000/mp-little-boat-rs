{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug client slint (LLDB)",
            "preLaunchTask": "rust: cargo build",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/little-boat-client",
            "cwd": "${workspaceFolder}",
            "args": [
                "${workspaceFolder}little-boat-client/src/main.rs"
            ]
        }
    ]   
}